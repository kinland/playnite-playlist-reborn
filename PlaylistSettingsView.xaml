<UserControl x:Class="Playlist.PlaylistSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <StackPanel Margin="12">
        <CheckBox
            VerticalAlignment="Center"
            IsChecked="{Binding ShowLastPlayedColumn}"
            Content="{DynamicResource LOCPlaylist_Settings_ShowLastPlayedColumn}" />
        <CheckBox
            Margin="0,8,0,0"
            VerticalAlignment="Center"
            IsChecked="{Binding ShowHowLongToBeatColumn}"
            IsEnabled="{Binding IsHowLongToBeatAvailable}"
            Content="{DynamicResource LOCPlaylist_Settings_ShowHowLongToBeatColumn}" />
    </StackPanel>
</UserControl>
